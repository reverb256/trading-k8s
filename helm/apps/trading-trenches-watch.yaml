apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trading-trenches-watch
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  source:
    repoURL: https://github.com/reverb256/trading-k8s.git
    targetRevision: main
    path: helm/charts/py-job
    helm:
      releaseName: trading-trenches-watch
      values: |
        name: trading-trenches-watch
        schedule: "8-59/15 * * * *"   # GMGN stagger: share the 15-min slot one job at a time
        command: ["python", "tools/gmgn_trenches_watch.py"]
        backoffLimit: 0   # never re-run a whole sweep: a retry is more requests
        deadline: 300
        repoMount: true
        configMount: true
        hostNetwork: false
        nodeName: nexus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - CreateNamespace=true
