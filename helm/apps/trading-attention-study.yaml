apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trading-attention-study
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  source:
    repoURL: https://github.com/reverb256/trading-k8s.git
    targetRevision: main
    path: helm/charts/py-job
    helm:
      releaseName: trading-attention-study
      values: |
        name: trading-attention-study
        schedule: "0 */6 * * *"
        command: ["python", "tools/attention_study.py"]
        deadline: 900
        repoMount: true
        configMount: false
        hostNetwork: false
        nodeName: nexus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - CreateNamespace=true
