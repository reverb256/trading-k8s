apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trading-reconcile-live
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  source:
    repoURL: https://github.com/reverb256/trading-k8s.git
    targetRevision: main
    path: helm/charts/py-job
    helm:
      releaseName: trading-reconcile-live
      values: |
        name: trading-reconcile-live
        schedule: "5 5 * * *"
        command: ["python", "tools/reconcile.py", "--wallet", "Ghr7xwP6HqzYVeXAq7uToR7poUSQuJ9pB8dCeQqA5wmf", "--log", "data/exec_log.jsonl"]
        deadline: 600
        repoMount: true
        configMount: false
        hostNetwork: false
        nodeName: nexus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - CreateNamespace=true
