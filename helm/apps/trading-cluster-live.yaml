apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trading-cluster-live
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  source:
    repoURL: https://github.com/reverb256/trading-k8s.git
    targetRevision: main
    path: helm/charts/py-job
    helm:
      releaseName: trading-cluster-live
      values: |
        name: trading-cluster-live
        schedule: "*/5 * * * *"
        command: ["python", "tools/cluster_live.py"]
        deadline: 240
        repoMount: true
        configMount: false
        hostNetwork: false
        nodeName: nexus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - CreateNamespace=true
