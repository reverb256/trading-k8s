apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trading-curve-watch
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  source:
    repoURL: https://github.com/reverb256/trading-k8s.git
    targetRevision: main
    path: helm/charts/py-job
    helm:
      releaseName: trading-curve-watch
      values: |
        name: trading-curve-watch
        schedule: "*/5 * * * *"
        command: ["python", "tools/curve_watch.py"]
        gpu: false
        deadline: 240
        env:
          - name: GOVERNED_POLL
            value: "1"
        trigger:
          kind: "external-poll"
          justification: "curve positions move with the RHC head; act only when the head moves"
          governor: "governed_poll"
          change-detection: true
          staleness-floor: "none"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - CreateNamespace=true
