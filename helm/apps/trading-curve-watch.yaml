apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trading-curve-watch
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  source:
    repoURL: https://github.com/reverb256/trading-k8s.git
    targetRevision: main
    path: helm/charts/py-job
    helm:
      releaseName: trading-curve-watch
      values: |
        name: trading-curve-watch
        schedule: "*/3 * * * *"
        command: ["python", "tools/curve_watch.py"]
        gpu: false
        deadline: 240
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - CreateNamespace=true
