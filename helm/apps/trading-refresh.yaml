apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trading-refresh
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: trading
  source:
    repoURL: https://github.com/reverb256/trading-k8s.git
    targetRevision: main
    path: helm/charts/py-job
    helm:
      releaseName: trading-refresh
      values: |
        name: trading-refresh
        schedule: "30 4 * * 0"
        command: ["python", "-u", "tools/research_refresh.py"]
        deadline: 900
        repoMount: true
        configMount: false
        hostNetwork: false
        nodeName: nexus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - CreateNamespace=true
